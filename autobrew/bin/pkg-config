#!/bin/sh
# Masks 'pkg-config' from PATH to always use static options
if [[ "$@" == *"--libs"* ]]; then
${BREWDIR}/opt/pkg-config/bin/pkg-config --static "$@" "$EXTRA_PKG_CONFIG_LIBS" \
	| sed 's#-lodbc#-lodbc -lltdl#g'
else
${BREWDIR}/opt/pkg-config/bin/pkg-config "$@"
fi
